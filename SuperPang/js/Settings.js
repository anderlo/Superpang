export default class Settings {
    static SCREEN_HEIGHT = 300;
    static SCREEN_WIDTH = 640;
    static GRAVITY = 9.8 * 100;
    static PLAYER_SPEED = 150;
    static HOOK_SPEED = 250;
    static HOOK_MAX = 3;
    static RADIUS_MIN = 5;
    static MARGIN = 12;
    static BONUS_DURATION = 5; // segundos que permanecerá
    static BONUS_SIZE = 20; // tamaño del Bonus
    static BONUS_SPAWN_CHANCE = 0.15; // probabilidad de obtener bonus despues de romper una bola
}